package com.example.smart_campus_operation_hub.service;

import org.springframework.stereotype.Service;

/**
 * MEMBER 4: Auth Service
 * TODO: Implement OAuth2 Google login, JWT generation, token refresh
 */
@Service
public class AuthService {

    // TODO: handleOAuth2Callback(OAuth2User oAuth2User) → JWT token
    // TODO: refreshToken(String refreshToken) → new JWT
    // TODO: logout(String token) → invalidate
    // TODO: findOrCreateUser(String email, String name, String avatarUrl, String providerId)
}
