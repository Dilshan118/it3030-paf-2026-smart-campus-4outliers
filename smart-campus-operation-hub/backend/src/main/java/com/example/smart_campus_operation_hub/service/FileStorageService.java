package com.example.smart_campus_operation_hub.service;

import org.springframework.stereotype.Service;

/**
 * MEMBER 3 (shared with Member 1 for resource images): File Storage Service
 * TODO: Implement file upload, validation, and storage
 */
@Service
public class FileStorageService {

    // TODO: storeFile(MultipartFile file) → returns file URL
    // TODO: deleteFile(String fileUrl)
    // TODO: validateFile(MultipartFile file) — check type (JPG/PNG), size (max 5MB)
}
