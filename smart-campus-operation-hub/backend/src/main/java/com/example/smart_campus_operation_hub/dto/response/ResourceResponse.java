package com.example.smart_campus_operation_hub.dto.response;

// MEMBER 1: Fields to return in API response (never expose the entity directly)
public class ResourceResponse {
    // TODO: id, name, type, capacity, location, description, availabilityWindows, status, imageUrl, createdAt
}
